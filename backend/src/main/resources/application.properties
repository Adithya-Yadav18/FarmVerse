# --- Database Configuration ---
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/farmverse_db
spring.datasource.username=root
# CHANGE THIS to the password you created during MySQL installation:
spring.datasource.password=Adithya5724@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA & Hibernate Configuration ---
# 'update' means Hibernate will automatically create our tables in MySQL based on our Java classes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Server Port ---
# Running our backend on port 8080
server.port=8080

# --- JWT Configuration ---
# This is our secret key to sign the tokens (must be at least 256 bits for HS256)
jwt.secret=2af47bb3342c423582cd9d50c2e21c733c83e3f0d1b39d3583c69e9b4c2882c8c91e35f728e4b8f6d2a321c4a5f6e7d8c9b0a1d2e3f4a5b6c7d8e9f0a1b2c3d4
# Token expires in 24 hours (in milliseconds)
jwt.expiration=86400000

# CORS Configuration: Comma-separated list of allowed URLs
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:8080
